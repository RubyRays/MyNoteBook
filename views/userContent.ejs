<%- include("partials/header"); -%>


<% newPage.forEach(function(newPageContent){%>
    <% if(newPageContent.title == userthing){%>
    <h1 class="page-entry">
        <%=newPageContent.title%>
    </h1>
    <p class="page-entry">
        <%=newPageContent.content%>
    </p>
    <%}%>
    <% }) %>

    <%- include("partials/footer"); -%>